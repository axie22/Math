# Work — 2026-08-27

Blank means hold; partial counts; wrong is the most informative thing you can leave.

Time started: ___
Time finished: ___

---

## 0. Review

### R1 — existential witness ($n<-3$, $n$ even)

Witness $n=$

Verification:

### R2 — plain English for $T(x,y)$: "$x<y$"

**(a)** $\forall x\exists y\,T(x,y)$ — true or false:

$\exists y\forall x\,T(x,y)$ — true or false:

**(b)** $\forall x\exists y\,T(x,y)$ claims (in words, about real numbers):

$\exists y\forall x\,T(x,y)$ would have required (in words), and why no real
number can do it:

---

## 1. Repair — prove $d_n = 4-2(-1)^n$ does not converge to $4$

My ε:

Arbitrary N. The n I construct from it:

Verification:

---

## 2. Core

### Core 1(a) — $T_2(x)$ and $R_2(x)$ for $\cos(x)$ at $a=0$

$T_2(x) =$

$R_2(x) =$

### Core 1(b) — prove $|R_2(x)| \le \dfrac{1}{6}$ on $[0,1]$

Bound and why:

Full argument:

### Core 2(a) — derive $S_n$ for $r=-\dfrac13$

$S_n =$

$rS_n =$

$S_n - rS_n =$ (show the cancellation)

$S_n =$

### Core 2(b) — closed form of $\sum_{k=0}^\infty \left(-\dfrac13\right)^k$

---

## Stretch (optional)

$\ln(1.5) \approx$ (using the degree-3 Taylor polynomial for $\ln(1+x)$)

Error bound, proved:

---

## Where I got stuck

*Two words is enough. "Ran out of time" and "no idea what to do after the setup" send
the next session in completely different directions.*

- Ran out of time at: ___
- Genuinely stuck on: ___

---

## Self-check

- Did I bound the derivative at $c$ using the *interval* $c$ lives in, not a guess? ___
- Did every "for all" in my plain-English answer name what the witness actually *is*? ___
- Did I ever try to figure out a sign instead of taking an absolute value? ___
- Time taken: ___ / difficulty (1 = trivial, 5 = lost): ___
