# Work — 2026-08-26

Blank means hold; partial counts; wrong is the most informative thing you can leave.

Time started: ___
Time finished: ___

---

## 0. Review

### R1 — setup line only (odd × odd = odd, direct proof)

Setup (hypothesis only — do not continue the proof):

### R2 — prove $b_n = 1+(-1)^n$ does not converge to $1$

My ε:

Arbitrary N. The n I construct from it:

Verification:

---

## 1. Repair — plain English, not transliteration

### (a) True/false for $R(x,y)$: "$y=x^2$"

$\forall x\exists y\,R(x,y)$ — true or false:

$\exists y\forall x\,R(x,y)$ — true or false:

### (b) What each one actually claims

$\forall x\exists y\,R(x,y)$ claims (in words, about real numbers):

$\exists y\forall x\,R(x,y)$ would have required (in words), and why no real
number can do it:

---

## 2. Core

### Core 1(a) — $T_2(x)$ and $R_2(x)$ for $e^x$ at $a=0$

$T_2(x) =$

$R_2(x) =$

### Core 1(b) — prove $|R_2(x)| \le \dfrac{e}{6}$ on $[0,1]$

Bound on $e^c$ and why:

Full argument:

### Core 2(a) — derive $S_n = \dfrac{1-r^{n+1}}{1-r}$

$S_n =$

$rS_n =$

$S_n - rS_n =$ (show the cancellation)

$S_n =$

### Core 2(b) — closed form of $\sum_{k=0}^\infty r^k$

### Core 2(c) — prove $(1/2)^n \to 0$ from the $\varepsilon$-$N$ definition

Given ε > 0, my N:

Verification:

---

## Stretch (optional)

$\sin(0.5) \approx$ (using the degree-3 Taylor polynomial)

Error bound, proved:

---

## Where I got stuck

*Two words is enough. "Ran out of time" and "no idea what to do after the setup" send
the next session in completely different directions.*

- Ran out of time at: ___
- Genuinely stuck on: ___

---

## Self-check

- Did I bound $e^c$ or $\sin(c)$ using the *interval* c lives in, not a guess? ___
- Did every "for all" in my plain-English answer name what the witness actually *is*? ___
- Time taken: ___ / difficulty (1 = trivial, 5 = lost): ___
