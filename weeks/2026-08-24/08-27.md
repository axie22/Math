# Session 8 — 2026-08-27 · Calculus repair, continued

**Phase 0, session 8 of ~15.** Level: **holding.** Session 7's Core (Taylor's
theorem, geometric series) never got reached yesterday — real work happened, but
time ran out at the repair block. That's a "no evidence" outcome for both new-material
items, not a "wrong" one, so per the standing rule they're held at the same level and
re-offered today with fresh instances rather than advancing to Induction. Induction I
slides to Monday 08-31; see `STATE.md` for the updated horizon.

One thing did land yesterday, and it's worth calling out directly because it's the
second time it's happened: a divergence proof got stuck at exactly the same step as
08-25's — trying to evaluate $(-1)^{\text{something}}$ by figuring out its sign,
instead of noticing $|(-1)^k|=1$ regardless of sign. That's today's repair item, on a
fresh instance. Full grading in [`08-26-feedback.md`](08-26-feedback.md); worked
solutions in [`08-26-solution.md`](08-26-solution.md).

Write your work in [`08-27-work.md`](08-27-work.md). **Timing box and "where I got
stuck" box, please** — third session running they've been left blank; even "ran out
of time at Core 2" is enough to stop the next session from guessing.

---

## 0. Review (~10 min)

**R1 — existential witness, closed book.** Prove: there exists an integer $n$ such
that $n<-3$ and $n$ is even. (This is the retirement check for existential-witness
domain, overdue since 08-22 — the habit of reaching for $\mathbb{Z}$, not
$\mathbb{N}$, when the statement doesn't restrict you to positive numbers.)

**R2 — re-offer, plain English (not a repeat of yesterday's predicate).** Let
$T(x,y)$ be "$x<y$" over $x,y\in\mathbb{R}$.

**(a)** True or false, no proof needed: $\forall x\exists y\,T(x,y)$? $\exists
y\forall x\,T(x,y)$?

**(b)** In a sentence each: what does $\forall x\exists y\,T(x,y)$ **claim about the
real numbers** (what is the $y$, in words, and can it depend on $x$)? What would
$\exists y\forall x\,T(x,y)$ **have required** — what single number would $y$ have
to be, and why can no real number do that?

---

## 1. Repair (~8 min) — the same obstacle, a new instance

Yesterday's $b_n=1+(-1)^n\not\to1$ got to $b_{N+1}=1+(-1)^{N+1}$ and stopped there,
about to split into cases on whether $N+1$ is even or odd. You don't need that split
— $\left|(-1)^{N+1}\right|=1$ no matter which it is. That exact shortcut is written
out in `08-25-solution.md` §2(c); today's version is built to force the same
question again: *do I actually need the sign here, or does $|\cdot|$ already give me
what I need?*

**Prove:** $d_n = 4-2(-1)^n$ does not converge to $4$.

(Same three moves: pick $\varepsilon$, take arbitrary $N$, construct an explicit
$n>N$, verify — without ever needing to know if $N$, or anything built from it, is
even or odd.)

---

## 2. Core (~30 min) — new material, held from yesterday, fresh instances

### Core 1 — Taylor's theorem, with a required proof

**(a)** Write the degree-2 Taylor polynomial $T_2(x)$ for $f(x)=\cos(x)$ at $a=0$,
and write the Lagrange remainder $R_2(x)$ explicitly (in terms of an unknown $c$).

**(b) — proof required.** Using $R_2(x)$, **prove** $|R_2(x)|\le\frac16$ for every
$x\in[0,1]$. You'll need a bound on the relevant derivative evaluated at $c$ — state
it and justify it in one line (it's shorter than yesterday's $e^c$ bound).

### Core 2 — geometric series

**(a)** Derive $S_n=\sum_{k=0}^n r^k=\dfrac{1-r^{n+1}}{1-r}$ for $r=-\dfrac13$, using
the multiply-and-subtract trick (not induction). Show the cancellation explicitly.

**(b)** Using (a) and citing (without reproving) that $r^{n+1}\to0$ when $|r|<1$,
write $\displaystyle\sum_{k=0}^\infty\left(-\frac13\right)^k$ in closed form.

*(No part (c) today — trimmed to fit what's actually been getting done in an hour
the last two sessions. The $\varepsilon$-$N$ convergence proof this would have
asked for is already covered by today's repair item and yesterday's Core 2(c) in the
solution file; it isn't dropped, just not re-asked back to back.)*

---

## Stretch (~10 min) — optional, skippable

Use the degree-3 Taylor polynomial for $\ln(1+x)$ at $a=0$ to approximate
$\ln(1.5)$ (i.e., $x=0.5$). Using the Lagrange remainder, **prove** your
approximation is accurate to within $0.02$.

---

## Notes

- **Core is a hold, not a repeat-for-repeat's-sake.** Yesterday's Taylor and
  geometric-series problems got zero evidence (blank from time, not tried and
  failed), so today asks the same two ideas with different functions/instances —
  this is what "no evidence → hold" looks like in practice, not "wrong → drill
  again."
- **Induction I (bug #2) moves from Thu 08-27 to Mon 08-31.** One-session slip,
  same mechanism as the 08-24 slip: new material doesn't start until the *previous*
  new material has actually been attempted once. `CURRICULUM.md`'s phase and gate
  are unaffected — this is pacing within Phase 0, not a trajectory change.
- Existential witnesses, contrapositive-vs-contradiction (concept), and both
  retirement-interval checks (Bug #1, pigeonhole) remain overdue beyond what fits
  in today's one review slot pair — Friday (08-28) is review day and is the natural
  place to clear more of this backlog.
