# Session 15 — 2026-09-07 · Monday (Phase 0, first session of a new week)

**Phase 0, session 15.** Last week closed at 2 of 5 sessions with real evidence —
08-31 and 09-01, both strong — followed by **three entirely blank sessions in a
row** (09-02, 09-03, 09-04), the most severe version of a pattern this repo has
been naming since 08-28. Nothing from that stretch is graded as wrong; it's all
held. Full detail in `STATE.md` and a new amendment in `CURRICULUM.md` §7 — worth
reading if you haven't looked at either in a while.

**Today's session is built a little differently because of that.** No repair
item — the divergence-proof problem is paused (see below). One review item, not
three, even though three retention checks are technically overdue — they're
spread across this week instead of stacked into one session. And Core is built
entirely around supremum/infimum, but not as a fourth cold problem: it leads with
a fully worked example this time, because three attempts at asking it three
different ways have all come back blank, and repeating a fourth cold version
isn't likely to do anything different.

Write your work in [`09-07-work.md`](09-07-work.md). Timing box and "where I got
stuck" — even one word changes what next session looks like.

---

## 1. Review (~10 min) — bug #2 retention, second attempt

The first retention check on induction's hypothesis-as-engine fix (09-04, sum of
cubes) came back blank — not wrong, just untested. Per the standing rule, one
more attempt before this drops out of active review:

**Prove by induction:** $\displaystyle\sum_{k=1}^{n}k^2=\frac{n(n+1)(2n+1)}{6}$
for all $n\ge1$.

Same shape as every prior instance: base case, then split off the $(n+1)$-th
term, substitute the hypothesis explicitly, and factor down to the target form.

*(Contrapositive-applied and plain-English quantifier meaning are also due for
their own one-time re-offer — they're not skipped, just scheduled for Tuesday and
Wednesday this week instead of crowded in today. See `REVIEW-QUEUE.md`.)*

---

## 2. Core (~40 min) — supremum, worked scaffold then a parallel instance

No repair item today, so this block gets extra room. Read the worked example
below in full before attempting anything — that's the point of it this time.

### 2a. Worked example (read this, don't just skim it)

**Claim:** $\sup\left\{1-\dfrac1n : n\in\mathbb{N}\right\} = 1$.

*Part 1 — upper bound.* For every $n\ge1$, $\frac1n>0$, so $1-\frac1n<1$. Hence
$1$ is an upper bound for the set.

*Part 2 — the $\varepsilon$-part (nothing smaller works).* Let $\varepsilon>0$ be
arbitrary. We need to find some element of the set bigger than $1-\varepsilon$.
By the Archimedean property, there exists $N\in\mathbb{N}$ with $\frac1N<\varepsilon$
(this is the same "find an $N$" move used in every $\varepsilon$–$N$ convergence
proof this month). Then the element $1-\frac1N$ of the set satisfies
$$1-\frac1N > 1-\varepsilon.$$
So no number smaller than $1$ is an upper bound — for any candidate $1-\varepsilon$,
we exhibited a set element that beats it.

Both parts done $\Rightarrow \sup\{1-\frac1n\}=1$. $\blacksquare$

**What to notice:** Part 1 is a one-line inequality. Part 2 is the part that
actually earns the word "supremum" instead of just "upper bound" — it's a direct
reuse of the $\forall\varepsilon\,\exists N$ pattern from the divergence-proof and
quantifier-order problems already retired this month. Sup/inf isn't new
machinery; it's the same $\varepsilon$–$N$ game applied to a different question
("what's the least upper bound" instead of "does this converge").

### 2b. Your turn — a parallel instance

**Prove:** $\inf\left\{1+\dfrac1n : n\in\mathbb{N}\right\} = 1$.

This is the mirror image of the worked example (infimum instead of supremum, so
"greatest lower bound" and the $\varepsilon$-part goes the other direction: find
a set element *below* $1+\varepsilon$... more precisely, below $1+\varepsilon$ for
the lower-bound version — work out which direction the inequality needs to go
before you start, the same way Part 1 vs. Part 2 above needed opposite-direction
inequalities). Structure your answer the same way: a lower-bound part, then an
$\varepsilon$-part using the Archimedean property exactly as above.

### 2c. Connecting sup/inf to what's already retired

**Prove:** if $A\subseteq\mathbb{R}$ is nonempty and bounded above with
$s=\sup A$, then for every $n\in\mathbb{N}$ there exists $a\in A$ with
$a>s-\frac1n$.

This is really just the $\varepsilon$-part of sup's definition with
$\varepsilon=\frac1n$ substituted in — the same specific-instance move used in
the worked example above, but now as a general claim about *any* bounded-above
set rather than one concrete sequence. It's also exactly the fact you'd need to
build a sequence inside $A$ that converges to $s$ (a preview of why sup/inf
matters for analysis beyond Phase 0, not a new proof technique).

---

## 3. Stretch (~10 min, optional — skip freely, not a prerequisite for Tuesday)

**Prove:** for any nonempty set $A\subseteq\mathbb{R}$ bounded below,
$$\inf A = -\sup(-A),$$
where $-A=\{-a : a\in A\}$.

This connects the two definitions directly instead of treating them as two
things to memorize separately. (Hint: showing $-\sup(-A)$ satisfies both parts of
the definition of $\inf A$ is more direct than trying to convert the definitions
symbol-by-symbol.)

---

## Notes

- **Divergence-proof execution (repair) — paused as of today**, two consecutive
  blank exposures (09-03, 09-04) after Friday's front-loading experiment produced
  no new information (the whole session was blank, not just that item). Not
  offered until a real-evidence session opens a slot ahead of it. See
  `STATE.md` and `CURRICULUM.md` §7 for the full accounting, including a flagged
  inconsistency in when this pause should have first applied.
- **Existential witnesses, strong induction, Taylor's theorem/geometric series**
  remain off active rotation, unchanged.
- **Backlog, still carried forward, not selected today:** setup discipline
  retention (due 09-02), pigeonhole general principle retirement check (due since
  08-24, oldest open item in the queue), quantifier negation — compound
  predicates retention (due 08-28), quantifier order retention (due 08-29),
  induction-counting ($2^n$ subsets) retention (due 09-02). None urgent — every
  one is a retention check on something already demonstrated clean at least
  once — but the queue is visibly backing up; see this week's review in
  `STATE.md`.
